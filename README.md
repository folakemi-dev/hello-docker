# Hello Docker

This project demonstrates building and running a simple Docker image using a shell script.

## Files
- Dockerfile – defines how the image is built
- app.sh – script executed inside the container

## Build image
```bash
docker build -t hello-docker .
